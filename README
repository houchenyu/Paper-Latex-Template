ACM Template and IEEE_Template folders contain the complete files for test if you want.
 
You can directly compile .tex if you have the correct environment.

`Ubuntu settings.md` is a backup for Ubuntu latex environment settings. 